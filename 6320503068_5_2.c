#include<stdio.h>
#include<math.h>
    void main(){
        int n,i,j;
        scanf("%d",&n);
        x = ceil(n/2);
        y = floor(n/2);
        if(n>=1&&n<=1000){
            for( i=0 ; i<x ; i++ ){
                for( j=0 ; j<n ; j++ ){
                    if
                }
            }
        }
    }
